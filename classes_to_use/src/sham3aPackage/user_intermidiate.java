package sham3aPackage;

public class user_intermidiate {
	user_interface u;
	user_intermidiate(user_interface u){
		this.u=u;
	}

	public void send_reg(user u) {
		
		// sends regestration using the user input           ----------->                                              class
		// then waits for a boolean to be sent
		// sends this boolean back to the view_registration_status function
		return;
	}

	public void send_login_req(user u) {
		// sends regestration using the user input ----------->                                              class
		// then waits for a boolean to be sent
		// sends this boolean back to the login_status function
		return;
	}

	public void send_rid_req(RideRequest r) {
		// sends ride_request using the RideRequest input  ----------->                                              class
	}
}
